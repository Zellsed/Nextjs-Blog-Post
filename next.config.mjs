/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.pexels.com",
      "ix-www.imgix.net",
      "ix-marketing.imgix.net",
    ],
  },
};

export default nextConfig;
